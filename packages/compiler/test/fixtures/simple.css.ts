import { select } from 'surimi';

select('.container').style({
  display: 'flex',
  flexDirection: 'column',
  padding: '1rem',
});

select('#header').style({
  backgroundColor: 'blue',
  color: 'white',
});
